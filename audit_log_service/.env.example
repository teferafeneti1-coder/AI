GRPC_SERVER_HOST=localhost
GRPC_SERVER_PORT=50051
POLL_INTERVAL=5
LOG_LEVEL=INFO
PLATFORM=auto
# PLATFORM can be 'windows', 'linux', or 'auto'

# HTTP inject endpoint — accepts login events from Agent Node login page
INJECT_HOST=0.0.0.0
INJECT_PORT=8081
