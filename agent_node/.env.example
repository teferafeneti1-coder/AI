AGENT_PORT=50052
HMAC_SECRET=your-secret-key-change-in-production
LOG_LEVEL=INFO

# Login page HTTP server (served from this machine)
LOGIN_API_HOST=0.0.0.0
LOGIN_API_PORT=8080

# Audit Log Service inject endpoint (on the Audit Log machine)
AUDIT_HOST=localhost
AUDIT_INJECT_PORT=8081
